{
  "name": "red-brick-corp-installer",
  "version": "1.0.0",
  "description": "Master controller and installer for the RBC project",
  "scripts": {
    "setup": "node setup.js",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}